// @flow
import React from "react";
import type { Node } from "react";

type Props = {
  color?: string,
  height?: number | string,
  className?: string,
  width?: number | string,
};

const Logo = (props: Props): Node => <></>;

export default Logo;
